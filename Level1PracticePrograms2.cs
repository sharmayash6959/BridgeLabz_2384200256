using System;

class Level1PracticePrograms2
{
	static void Main(string[] args)
	{
		string name = "Sam";
		int maths = 94, physics = 95, chemistry = 96;
		double averageMarks = (maths + chemistry + physics)/3;
		
		Console.WriteLine(name+"'s average marks in PCM is "+averageMarks);
	}
}